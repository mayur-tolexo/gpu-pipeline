apiVersion: v1
kind: Service
metadata:
  name: mq-service
  namespace: gpu-pipeline
  labels:
    app: mq
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: mq